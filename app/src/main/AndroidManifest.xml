<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RatherLLM">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.kotlin.ratherllm">

<!-- Core foreground-service permission (API 28+). -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

<!--
    Android 14+ (API 34) requires a typed FGS permission. For a local,
    always-resident AI inference engine the grantable type is SPECIAL_USE.
    (SYSTEM_EXEMPTED is NOT available to ordinary apps.)
-->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

<!-- Show the ongoing notification on Android 13+ (runtime-requested). -->
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<!-- Keep the CPU running for the decode thread while the screen is off. -->
<uses-permission android:name="android.permission.WAKE_LOCK" />

<application
    android:name=".RatherLlmApp"
    android:label="@string/app_name">

    <!-- ... your activities ... -->

    <service
        android:name=".InferenceService"
        android:exported="false"
        android:foregroundServiceType="specialUse"
        android:stopWithTask="false">

        <!--
            Required for the SPECIAL_USE type on API 34+. The value is a
            human-readable justification reviewed by Google Play.
        -->
        <property
            android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
            android:value="On-device local LLM/MLLM inference engine that keeps quantized model weights memory-locked (mlock) and a CPU-pinned decode thread resident for low-latency, fully-offline token generation." />
    </service>

</application>
</manifest>
